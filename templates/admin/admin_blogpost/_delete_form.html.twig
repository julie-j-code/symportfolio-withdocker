<form class="mt-5" method="post" action="{{ path('admin_blogpost_delete', {'id': blogpost.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blogpost.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
